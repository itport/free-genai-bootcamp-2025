# Lang Portal React Client

React and TypeScript interface for the Flask Lang Portal API.

## Run locally

```sh
npm install
npm run dev
```

The client uses `http://localhost:5000` by default. To use another backend:

```sh
VITE_API_BASE_URL=https://api.example.com npm run dev
```

## Checks

```sh
npm run lint
npm run build
```
