# Route 2 — o1-preview

This route uses o1-preview to generate an alternative system prompt for the same English-to-Romanian tutoring task.

See `Request.md` for the original request, `prompt.md` for the generated system prompt, and `01-mini-example.md` for an example response.
